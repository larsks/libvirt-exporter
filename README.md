# libvirt exporter for prometheus

Exports domain statistics for scraping by prometheus.

## Usage

```
Usage: libvirt-exporter [OPTIONS]

Options:
  -u, --uri TEXT
  -l, --listen TEXT
  -p, --port INTEGER
  -x, --labels-from-xml PATH
  -v, --verbose
```
